<Window x:Class="DesktopPrivacyShield.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DesktopPrivacyShield"
        Width="760"
        Height="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="28" FontWeight="SemiBold" Text="Desktop Privacy Shield" />
        <TextBlock Grid.Row="1"
                   Margin="0,10,0,24"
                   Foreground="#666666"
                   Text="Windows 桌面隐私遮罩工具。遮罩模式不会替代系统级锁屏。"
                   TextWrapping="Wrap" />

        <Border Grid.Row="2" Padding="22" Background="#111111" CornerRadius="6">
            <StackPanel>
                <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="White" Text="当前状态" />
                <TextBlock Margin="0,12,0,0" Foreground="#D0D0D0" Text="{Binding IsLocked, StringFormat=已遮罩: {0}}" />
                <TextBlock Margin="0,8,0,0" Foreground="#AAAAAA" Text="使用托盘菜单或 Ctrl+Alt+L 可以快速进入遮罩。" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Margin="0,24,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="120" Height="38" Margin="0,0,12,0" Content="设置" Click="OnOpenSettingsClicked" />
            <Button Width="120" Height="38" Content="立即遮罩" Command="{Binding LockCommand}" />
        </StackPanel>
    </Grid>
</Window>
